# 万界罗盘 Skill 验收记录

## 结构校验

- 源目录：`Skill is valid!`
- 全局安装目录：`Skill is valid!`
- 源目录与全局副本：`diff -qr` 无差异
- 品牌迁移后机器标识：`wanjie-luopan`
- 旧源码与全局目录：均已移除

## 脚本回归

执行：

```text
python3 -m unittest discover -s wanjie-luopan/scripts -p 'test_*.py' -v
```

结果：4 项通过。

- 高质量候选进入 `look-now`
- 无实质代码候选被硬门槛排除，即使总分为 100
- 越界评分被拒绝
- 数组和 `{ "candidates": [...] }` 两种输入均可读取

## 全局副本回归

在当次测试用户的 `~/.codex/skills/wanjie-luopan` 全局副本中重复执行 4 项脚本测试，全部通过。

## 独立前向测试

测试请求：寻找适合 macOS 本地运行、用于研究语音转文字的开源项目；只做快速发现，不安装运行。

结果：通过。

- 从 15 个真实候选收敛为 5 个“立即值得看”和 3 个备选
- 正确混合底层引擎、Apple 原生 SDK、跨模型框架和少量产品参考，并解释项目形态差异
- 所有候选标为 E1 仓库表面证据
- 明确未验证准确率、中文效果、速度、功耗和芯片兼容性
- 没有克隆、安装、编译、下载模型或运行代码
- 没有讨论变现，也没有强行给唯一最优项目
- 识别到 `whisperkit` 已迁移到 `argmax-oss-swift`，并拆分开源版与 Pro 能力
- 识别 GitHub 搜索中的大量同类薄封装并做合并去重

发现的摩擦：

- “研究语音转文字”可能指算法/推理，也可能指完整听写应用。Skill 通过显式默认假设处理，没有阻塞搜索。
- 项目改名、重定向和开源/商业版能力边界需要回到原始仓库逐项核对，不能只依赖搜索结果。

## 验收结论

Skill MVP 满足 AC-01 至 AC-11，可以作为全局个人 Skill 使用。当前证据只证明工作流和脚本可用，不代表未来所有搜索主题都能稳定达到相同质量；继续用真实任务迭代。

2026-07-25 品牌迁移后，源码与全局副本再次通过结构校验和各 4 项脚本回归；旧机器标识无残留。巡天、星历、藏卷和舆图仍属于产品规划，不计入当前验收范围。

## 2026-07-28 仓库装修回归

本轮在不修改 Skill 核心规则的前提下，重构 README、上手与排错文档、真实样例、品牌视觉和 CI。

结果：

- 点睛仓库审计：0 errors、0 warnings
- `quick_validate.py`：`Skill is valid!`
- 评分器回归：4 项通过
- 临时干净安装副本：结构校验和 4 项回归通过
- README 本地链接和图片链接：全部可解析
- SVG：`xmllint` 校验通过
- YAML：Skill 元数据与 GitHub Actions workflow 可解析
- README 桌面首屏：1280×900 渲染通过
- README 窄屏首屏：390×844，无水平页面溢出
- 新 Social Preview：1280×640，约 88 KB
- 新 Hero：1440×720，约 134 KB
- 根目录与 Skill 包内的 Logo、图标副本一致

本轮没有推送，没有创建 Tag / GitHub Release，没有上传新的 Social Preview，也没有修改 GitHub Settings。公开仓库仍显示上一版远端内容。

## 2026-07-29 公开发布候选回归

- 装修提交已推送到公开仓库 `main`
- GitHub Actions `Validate Skill` 在公开提交上运行成功
- 从公开仓库全新浅克隆 `main`，没有复用本地工作树
- 按上手文档将 Skill 安装到新的临时目录
- 公开源码包和临时安装副本 `diff -qr` 无差异
- 临时安装副本通过 `quick_validate.py`
- 公开克隆中的评分器 4 项回归测试全部通过
- 既有独立前向测试仍作为第一次成功的功能证据；本次没有在已打开任务中冒充 Skill 热刷新
